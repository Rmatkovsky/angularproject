# AngularJS forms (test task)

## install server
      npm install -g browser-sync

## install node modules
      npm install

## run server
      node api/app.js

## run frontend
      browser-sync start --server
